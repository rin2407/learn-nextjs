import React from 'react';
import PropTypes from 'prop-types';

Layout.propTypes = {
    
};

function Layout(props) {
    return (
        <div>
            <h5>trang lay out</h5>
        </div>
    );
}

export default Layout;